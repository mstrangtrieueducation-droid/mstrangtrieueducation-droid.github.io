Submission regression tests use synthetic identities and intercept every network request. They verify immutable attempts, receipt integrity, storage failure, automatic recovery, and cross-tab confirmation. The tested source is the shipped runtime asset. Run with Node.js and Playwright, then `node tests/ielts-submission/client.test.cjs` and `node tests/ielts-submission/cross-tab.test.cjs`.
